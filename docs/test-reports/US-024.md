# Test report US-024 – Engine/game split (D-006)
Result: PASS

Environment: clean worktree `C:\dev\projects\game_project_test` @ commit fc03cfe (isolated from an unrelated programmer's uncommitted US-028 work in the main repo). Served via `python -m http.server 8790` from the worktree root; server stopped at the end of the session.

Criteria:
- [x] Layout matches D-006 (`engine/{core,render,world,physics,entities,ui}/`, `game/{index.html, world-test.html, physics-test.html, js/main.js, js/dev/, js/ui/}`)
- [x] `game/js/{engine,render,world,physics,entities}/` no longer exist – confirmed; only `dev/`, `main.js`, `ui/` remain under `game/js/`
- [x] `node tools/check-deps.mjs` – `check-deps OK (57 files)`
- [x] `node tools/check-deps.test.mjs` – 8/8 pass (correctly flags all 6 negative fixtures: window.ASSETS/global reads in engine/, bare Node built-ins, imports escaping engine/, deep imports bypassing engine/index.js, design/ using import/export)
- [x] `node engine/physics/physics.test.js` – 187/187
- [x] `node engine/physics/jump.test.js` – 111/111
- [x] `node engine/entities/eyeFeel.test.js` – 18/18
- [x] `node engine/core/playerLook.test.js` – 10/10
- [x] `node --expose-gc tools/bench-cast.mjs --gc` – ALL CHECKS PASS, all 5 poses at 9,600/9,600 cell writes, embedded reference checksums matched, 0 GC/0 scavenge, heap deltas well under the 2048 B/frame budget, p50 0.95–1.30 ms (well under budget)
- [x] `game/index.html` in all 6 URL modes, no console errors:
  - default – renders `test_room`, pause overlay shown (pointer not locked, expected)
  - `?debug=1` – overlay shows fps 59.9, frame/grid-draw times, backend gl2, live pos/yaw/pitch
  - `?demo=1` – gradient/glyph demo renders
  - `?glyphs=1` – glyph atlas grid renders
  - `?bench=1` – logs `[bench] US-001 worst-case, 600 frames`, present() avg 0.05 ms/p95 0.1 ms (well under budget); full-frame avg ~18 ms is this mode's own known worst-case cost (unrelated to this story, same as prior baselines)
  - `?shadetest=1` – logs `[shadetest] 361/361 pass, worst deviation 0.25` / `ALL PASS`
  - bonus spot-check: `?force2d=1` – clean, no errors
- [x] `game/world-test.html` – loads, shows real `test_room` sector data with hover-inspect grid, no console errors
- [x] `game/physics-test.html` – loads, shows live top-down capsule/collision HUD against real `test_room`, no console errors

Bugs: none found.

Performance: bench-cast p50 0.95–1.30 ms/pose (budget headroom large, no escalation trigger); `?debug=1` overlay reported 59.9 fps; `?bench=1` present() cost flat (~0.05 ms avg) confirming no regression from the engine/game split.

Console errors: none in any of the 6 index.html modes, world-test.html, or physics-test.html.

Suggestions (non-blocking): none.
