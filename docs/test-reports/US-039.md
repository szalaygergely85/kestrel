# Test report: US-039 voxel model format + JS oracle

**Date:** 2026-09-23. **Tested by:** main session (Node-level checklist; three commands, run directly to save tokens). **Commit:** caa68ee, in the clean worktree `../game_project_test`.

| Check | Result |
|---|---|
| `node --expose-gc engine/voxel/voxel.test.js` | **64 pass, 0 fail** (format validation, oracle determinism, zero-alloc) |
| `node --expose-gc tools/bench-voxel.mjs` | heapDelta/call about -4.6 B (zero-alloc OK). Timing 160x60: p50 0.39 ms, p95 0.70 ms, over the 0.3/0.5 ms figures; per the PO's AC update this is **informational** for the CPU oracle (US-040's GPU pass is the perf gate). Machine is noisy. |
| `node tools/check-deps.mjs` | **OK** (97 files) |

**Result: PASS.**
