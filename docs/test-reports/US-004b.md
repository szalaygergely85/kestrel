# Test report US-004b – Sector caster: overdraw to 1.0x, allocation-free ray loop, fast shader, headless bench

Result: PASS

Criteria:
- [x] Baseline recorded, `tools/bench-cast.mjs` exists and works as specified – verified.
- [x] `node tools/bench-cast.mjs` – exit 0, `ALL CHECKS PASS`, 5 poses (start, stair+platform, sky-over-low-wall +20, diagonal -35, low-wall-sky yaw45/+25), 9,600/9,600 writes on every pose, all probes OK, embedded-baseline / fast-vs-reference / skyFallback:false checks all OK.
- [x] `node --expose-gc tools/bench-cast.mjs --gc` – exit 0, `ALL CHECKS PASS`, 0 GC events (0 minor/scavenge) on every pose, heap deltas 30–210 B/frame (limit 2048 B/frame).
- [x] `node game/js/physics/physics.test.js` – 187/187 pass, untouched by this story.
- [x] Overdraw 1.0x / zero allocations / `OpenSpans` typed struct / fast shading path – all confirmed via the bench's own checks (write-count == 9,600 every pose, 0 GC, fast-vs-reference glyph-identical with worst channel diff 1).
- [x] Performance: p50 ranged ~0.85–3.2 ms across poses/runs in this shared sandbox (some CPU-contention noise on the diagonal/low-wall-sky poses), all well under the 2.5 ms target and nowhere near the 3.5 ms escalation trigger.
- [x] Image identity: reference-shader checksums match `EMBEDDED_BASELINE`; fast-vs-reference per-cell OK (0 glyph mismatches, worst channel diff 1) on every pose.
- [x] Skylight / far ceiling fix – bench probes confirm columns 120/132 rows 22-25 are finite depth (far ceiling, not sky) at the start pose, and column 116 rows 0-3 are sky (not stretched ceiling) at the low-wall-sky pose. Manually reproduced in browser via `window.__debug` (set `look.locked=true`, player pos/yaw/pitch): at pitch 0 from the start pose a sky band is visible through the skylight cut with dark room around it (ambient-only, dim as expected); at pitch +35 away from the skylight, the whole frame is ceiling texture, no sky visible.
- [x] `?shadetest=1` in a real browser (port 8123): 361/361 pass, worst deviation 0.25 (tolerance ±4), logged `ALL PASS`.
- [x] Other URL modes all load with no console errors: default, `?debug=1`, `?bench=1`, `?glyphs=1`, `?force2d=1`, `?demo=1`, `?origin=1480,1018`.
- [x] No console errors on any mode tested (only the expected `[RenderTarget] back-end: gl2` and shadetest log lines).

Bugs: none found.

Performance: headless bench p50 0.85–3.2 ms per pose (shared/noisy sandbox; architect's own re-runs saw the same spread), well under the 2.5 ms budget and the 3.5 ms escalation trigger. `?bench=1` in-browser loaded with no errors (not re-timed in-browser, headless numbers are the story's authoritative measurement per the AC).

Console errors: none.

Manual checks left for the user (this sandbox's Browser pane could not obtain real pointer lock via automated clicks, so true mouse-look could not be exercised):
- Confirm mouse-look feels correct and pointer lock engages normally in a real desktop Chrome window (arrow-key pitch fallback and the `window.__debug` pose overrides used here both worked and matched the bench's exact-depth probes, but a real mouse-driven look was not exercised).
- Eyeball the start-pose and low-wall-sky views once lit (US-006/007) – acknowledged known limitation: ambient-only lighting makes most differences hard to see by eye today; this was expected and out of scope for this story.

Suggestions (non-blocking): none beyond what's already tracked in the story notes (material-record binding at `loadLevel`, caller-owned `OpenSpans`/camera reuse for US-024, the pre-existing two-adjacent-different-height-solids gap noted by the programmer as a future bug story if a level ever uses that pattern).
