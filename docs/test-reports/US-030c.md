# Test report US-030c – GPU sprite pass + JS `drawSprites` reference

Result: **PASS, waiting on US-030a** (US-030a's `?gpucompare=1` FAILed this session on BUG-GPU-002/BUG-CPU-001, both geometry-level and provably sprite-independent — see below. This story's own ACs are met; `done` is blocked on the dependency per its own checklist item 6.)

Environment: clean worktree `game_project_test` @ 37966f1, fresh port 8920 (stopped after), ANGLE Intel 0x7D45 D3D11.

Criteria:
- [x] Architect tech notes (section 14.2).
- [x] GPU sprite pass: `game/index.html?level=test_room&sprite=1&debug=1` shows overlay `sprites: 3`, brazier (lit flame, full colour), lantern near, far lantern; drawn over GPU-shaded surfaces; 0 console errors.
- [x] `game/sprites.html?spritecompare=1&source=upload`: **5/5 PASS**, frame glyph 100% on all poses, sprite cells 121/157/63/0/4 across poses, glyphMismatch 0, fgMax 0.
- [x] `?gpu=0` fallback (`sprites.html?sprite=1&gpu=0`): JS `drawSprites` draws the same three props (brazier flame + 2 lanterns) on `path: cpu`/backend `gl2`, no errors.
- [x] `WEBGL_lose_context` lose/restore on `sprites.html?sprite=1`: sprites redraw after restore (brazier + far lantern glyphs visible, `sprites: 3` on overlay). Minor/non-blocking: on this dev-only page the surrounding wall surfaces stayed black post-restore (world atlas isn't rebuilt in this ~100-line harness); `game/index.html`'s own context-loss/restore (tested under US-030a) fully rebuilds and is unaffected — not a regression in this story.
- [x] Removing a sprite entity: `world.removeEntity('test_brazier')` → the brazier disappears on the next frame, overlay drops from `sprites: 3` to `sprites: 2`. Stale-cache fix confirmed.
- [x] `node --expose-gc engine/render/gpu/sprites.test.js`: 49/49. `node engine/world/world.test.js`: 20/20. `node tools/check-deps.mjs`: OK (88 files).
- [~] `?gpucompare=1` rerun of spawn/stair poses "without" the compare sprites: not re-run with sprites literally disabled (main.js's compare block has no such toggle), but verified by code inspection that `compareGeometry(gbuf, depthBuffer.depth, GI, GA, Depth, ...)` — the function producing the `holes`/`kind%` figures for both FAILing poses — takes no sprite-pool input at all; sprites only affect the separate `compareCells` (colour) path via `rt.present()` compositing. So both FAILs (world_m1 spawn = BUG-CPU-001, facing-stair = BUG-GPU-002) are geometry-only and structurally cannot change with sprites removed. Consistent with the architect's and programmer's own prior confirmations in the backlog. Not a US-030c regression.

Bugs: none new for this story. (BUG-GPU-002 and BUG-CPU-001 are US-030a's, see US-030a.md.)

Performance: sprite pass gpu ~0.10-0.14ms/frame in `game/index.html`; no budget concerns.

Console errors: none on any sprite page/mode tested.

Suggestions (non-blocking):
- `game/sprites.html`'s context-loss handling doesn't rebuild the world texture atlas (only sprites redraw) — fine for a throwaway dev page, but worth a one-line doc note so it isn't mistaken for a real regression later.

## Re-test 2026-09-23 (commit a827b2c, port 8922)

Result: **PASS, still waiting on US-030a** (unchanged verdict — dependency still not `done`).

This story's own ACs remain met: `?level=test_room&sprite=1&debug=1` shows `sprites: 3` (brazier + 2 lanterns) over GPU-shaded surfaces, 0 console errors; `node --expose-gc engine/render/gpu/sprites.test.js` 49/49; `node engine/world/world.test.js` 20/20; `check-deps.mjs` OK (88 files).

Re-ran `?gpucompare=1` (which includes sprite cells): still 5/7, same two geometry FAILs as US-030a's re-test (`start pose` holes=1, `facing stair` holes=5) — both confirmed geometry-only via `window.__gpuCompare` (`cmpGeom.holes`, sprite-independent per `compareGeometry`'s inputs, as established in the original test). BUG-CPU-001's row (`world_m1 player spawn`) now PASSes, consistent with US-030a's re-test.

**Verdict unchanged: stays `testing`, not `done`.** US-030a's own re-test FAILed (BUG-GPU-002 reopened, worse than originally reported), so per this story's checklist item 6 ("`done` also requires US-030a `done`"), US-030c cannot go to `done` this session either. No new bugs for this story.

## Re-test 3 (2026-09-23, worktree @ e0f0e94, fresh port 8925)

Result: **PASS -> `done`** (dependency US-030a now `done`, see its re-test 3).

`?gpucompare=1` (sprite-inclusive) now 7/7 PASS with the fixed 1280x720 reference camera; the two geometry FAILs from the previous re-test were a window-size artifact of the old reference camera (per US-030a's re-test 3), not a US-030c or sprite-pass issue. This story's own ACs re-confirmed: `?level=test_room&sprite=1&debug=1` shows `sprites: 3`, GPU path, 0 console errors; no new bugs.

**Verdict: US-030c -> `done`.**
