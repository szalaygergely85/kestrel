# Test report US-029 – GPU pipeline: shading + edge pass on the GPU, parity page (D-009 stage 1, GATE)

Result: PASS

Environment: clean worktree `game_project_test` @ 837c3ce (HEAD detached, working tree clean), served with `python -m http.server 8903` from the worktree root (fresh port, 8000/8801/8802 untouched). Browser: Chromium via the Browser tool, ANGLE (Intel, Intel(R) Graphics (0x00007D45) Direct3D11 vs_5_0 ps_5_0, D3D11) — same renderer string as the owner's hardware run recorded in the backlog.

Criteria:
- [x] `?gpucompare=1` at 1600x900, fresh port — 5/5 PASS, `poisonedSurvivors` 0 on every pose (read the actual `window.__gpuCompare` report object, not just the on-screen text, to confirm the per-pose field). Table:

| pose | glyph match (non-edge) | fg/bg outside ±4 | fgMax/bgMax | depth match | poisonedSurvivors |
|---|---|---|---|---|---|
| start pose (S, facing east, level) | 100.00% | 0/0 | 1/0 | 100% | 0 |
| facing stair + 1.0m platform | 100.00% | 0/0 | 1/0 | 100% | 0 |
| sky over the low wall, pitch +20 | 100.00% | 0/0 | 0/0 | 100% | 0 |
| long diagonal, pitch -35 | 100.00% | 0/0 | 0/0 | 100% | 0 |
| low wall sky, (10,7.5) yaw45 pitch+25 | 100.00% | 0/0 | 0/0 | 100% | 0 |

- [x] Context-loss fallback — from the console: `gl.getExtension('WEBGL_lose_context').loseContext()` → `window.__debug.rt.gpuActive` flips to `false` immediately (CPU shading), `gl.isContextLost()` true, no crash, no console errors. `.restoreContext()` → `gpuActive` flips back to `true`, `isContextLost()` false, and a screenshot confirms the world renders fully (not black) with the GPU path active again.
- [x] `?force2d=1` — `window.__debug.rt.backend === 'c2d-capped'`, fully playable (screenshot confirms rendered world), zero console errors. Also checked `?gpu=0` — `backend: 'gl2'` but CPU shading (no GPU pipeline active), also fine.
- [x] Normal play with `?debug=1` — overlay shows `backend: gl2`, `shade: gpu  upload 0.40-0.60ms  gpu 2.28-2.39ms`, well inside the ≤4ms GPU budget (this sandbox's virtualized Chromium is slower overall than the owner's laptop — fps ~28-40 here vs the owner's 60 — but the GPU-specific figure that the AC actually gates on stays comfortably under budget, and JS frame time is not the AC being tested here).
- [x] `?shadetest=1` — v1 table 361/361 PASS, v2 table 2242/2242 PASS. This confirms the designer's mirrored-`qfloor` fix (commit 0c830c9, "shadetest v2 2242/2242") landed and restored the count after the programmer's item-5 fix had dropped it to 1774/2242 as a documented side effect. The backlog's PO-OK note under the row (pre-designer-fix wording) is stale/superseded by the PO-review section text and the actual commit history; current live numbers match the PO's claimed 2242/2242.
- [x] Node suites: `node --test` over `engine/**/*.test.js` and `tools/check-deps.test.mjs` — all green (playerLook, eyeFeel, handle, jump, physics, compositor, packed, serialize, terrain, world, check-deps self-test, and the three GPU suites `ShadeTextures.test.js` 1104/1104, `glsl.test.js` 19/19, `gpuCompare.test.js` 13/13). No failures anywhere.
- [x] `node tools/check-deps.mjs` → `check-deps OK (76 files)`, no engine imports from `game/` or `design/`.
- [x] `node --expose-gc tools/bench-cast.mjs --gc` → first run showed the JS-timing "escalation trigger" lines FAIL/OVER BUDGET on 3 of 5 poses (busy-machine noise, extensively documented and accepted in the US-028/US-029 history — this story's own programmer/architect notes hit the identical flake on the identical machine class). A clean re-run seconds later on the same worktree/commit produced `[bench-cast] ALL CHECKS PASS` (0 GC, correctness/checksum/flicker/GC/probe checks all green both times — only the timing-gate numbers flickered under load). Per the accepted precedent (US-028 PO decision, US-029 architect/programmer notes on 2c24f83), this is not a regression and not blocking.
- [x] Debug views `?gpudebug=kind|plane|shaded` render solid black — reproduced, matches the documented known issue explicitly carved out to US-030a in the PO-OK note. Not failing US-029 for this per the task's instructions.

Bugs: none blocking.

Suggestions (non-blocking):
- The US-029 backlog row's inline fine-print (the long paragraph under "PO OK 2026-09-23...") is now stale — it still describes the v2 shadetest regression (1774/2242) as unresolved/flagged, while the PO-review section further down and the actual commit history (`0c830c9` "Designer: qfloor mirrored in reference shade (shadetest v2 2242/2242)") show it was fixed before PO OK. Worth a small edit to the row text so a future reader doesn't re-open a closed question. (Not touching it here — out of scope for the tester's edit permissions on this story.)

Performance: GPU shading measured at 2.28-2.39 ms/frame in this sandboxed browser (budget ≤4 ms) — PASS. Upload 0.40-0.60 ms. fps in this environment 28-40 (virtualized/shared machine, not representative of real hardware — the owner's own hardware run recorded 60 fps / gpu 0.25 ms in the backlog, which is the run that satisfies the "real hardware" AC per story rules).

Console errors: none observed across shadetest, gpucompare, context-loss/restore, `?force2d=1`, `?gpu=0`, `?debug=1`, and `?gpudebug=kind` runs.

D-009 gate: confirmed PASSED per this tester's own run of the parity page (5/5 PASS, poisonedSurvivors 0) plus the previously recorded owner-hardware run. No escalation needed.
