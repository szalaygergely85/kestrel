# Test report US-030a – GPU raycasting: GLSL DDA + configurable grid

Result: **FAIL**

Environment: clean worktree `game_project_test` @ 37966f1, fresh port 8920 (stopped after), ANGLE Intel 0x7D45 D3D11 (matches the owner's GPU). Node v24.21.0.

Criteria:
- [x] Architect tech notes written before dev.
- [x] Sector DDA in GLSL writes the G-buffer; CPU caster does not run on `gl2`.
- [ ] **Parity `?gpucompare=1`: 6/7 PASS, holes = 0 on all 7 — NOT MET.** Reproducibly got only 5/7 clean PASS across 4 reloads: `test_room facing stair + 1.0m platform` FAILs with **holes = 1** (geometry check, must be 0) every time after the first load stabilizes (first cold load showed holes = 13 on that pose and holes = 1 on the start pose too, then settled to a consistent holes=1 on the stair pose only on 3 subsequent reloads). `world_m1 player spawn` is the documented known FAIL (BUG-CPU-001, kind 99.92-99.93%, not blocking). See BUG-GPU-002 below.
- [x] Configurable grid: no param → `grid: 240x90`; `?grid=160x60` and `?grid=320x120` render, glyphs legible at 160/240 (320 cells too small to read at this viewport, as previously flagged to the owner — expected, not a bug).
- [x] Clamping: `?grid=400x200` → clamped to 320x120, logged once (console warn). `?grid=100x40` → clamped to 160x60, logged once.
- [x] Fallbacks: `?gpu=0` → gl2 presenter, `path: cpu` (JS shading), grid 160x60. `?force2d=1` → backend `c2d-capped`, `path: cpu`, grid 160x60, playable.
- [x] Context loss/restore: `WEBGL_lose_context` → `path: cpu`, `gpu: n/a`, canvas blanks (expected drop). `restoreContext()` → `path: gpu` again, world fully re-rendered, no crash.
- [x] **BUG-OWN-001 fix confirmed**: teleport to (1500.69, 1027.36, 3.00) yaw 236 pitch -29 — GPU shows wall/step/floor, matches `?force2d=1` (also confirmed via the gpucompare row: 100% PASS every run). Row set to `fixed`.
- [x] No console errors on any page tested (grid variants, fallbacks, context loss, gpucompare). Only expected `[warn] [grid] ... clamped` and `[log] [gpucompare] ...` lines.
- [x] `node tools/check-deps.mjs`: OK (88 files).
- [x] All Node suites green: `physics`, `jump`, `eyeFeel`, `playerLook`, `gpuCompare` (35/35), `WorldTextures` (12/12), `sprites` (49/49), `world` (20/20), `ShadeTextures` (1104/1104), `compositor` (10/10), `glsl` (33/33), `behaviours` (9/9), `handle` (15/15), `packed` (5/5), `serialize` (9/9), `terrain` (19/19), `tower` (49/49), `check-deps.test.mjs` (8/8 fixture PASS).
- [x] `bench-cast --gc`: checksums (fast + v2, glyph/fg/bg) unchanged vs the embedded baseline on both runs — no regression. Timing budget lines ("OVER BUDGET"/escalation-trigger FAIL) reproduced on both of two consecutive runs on this sandbox; matches the pre-existing, previously-documented busy-machine timing flake (US-028/US-029/US-030a notes) and is unrelated to checksums.

Bugs:
- **BUG-GPU-002 [major] `?gpucompare=1` stair pose fails holes=0 gate, reproducibly**
  Steps: 1. Serve worktree, open `game/index.html?gpucompare=1`. 2. Read the `test_room: facing stair + 1.0m platform` row (repeat: reload 3-4x).
  Expected: holes = 0 on all 7 poses (architect review 3 ruling, PO OK basis).
  Actual: holes = 1 on every reload after the first (first cold load: holes = 13 on this pose, holes = 1 on the start pose too — settles down but never reaches 0 on the stair pose in 4 attempts). Geometry-only (`compareGeometry` reads `gbuf`/`depthBuffer`/GI/GA/Depth, no sprite/color data), so this is a DDA/CPU-oracle boundary defect, not related to US-030c's sprite pass.
  This contradicts architect review 3 ("Holes = 0 on all 7 poses without [item 2]") and the PO OK's stated basis, so US-030a does not currently meet its own parity AC in this environment. Recommend ASK ARCHITECT to re-probe the stair pose specifically (same class of boundary issue as review 2's items 1/2, revisited).

Performance: `?debug=1` @240x90 default: gpu ~1.1-2.1ms/frame (well under 4ms budget); fps in this sandboxed headless Chrome is low (10-20fps) as previously documented (slower sandbox, not the gated metric). 320x120: gpu ~1.6-1.9ms, still under budget.

Console errors: none on any page tested.

Suggestions (non-blocking):
- The holes counter's apparent non-determinism across cold loads (13 → 1 → 1 → 1) versus review 3's clean 0/7 suggests either GPU-driver/timing sensitivity in the readback or a boundary case that only manifests on this machine's ANGLE build — worth a note in architecture.md 14.2 if it recurs elsewhere.

## Re-test 2026-09-23 (commit a827b2c, port 8922)

Result: **FAIL** (BUG-GPU-002 not fixed; regressed).

Environment: clean worktree `game_project_test` @ a827b2c (one commit after 980a405, the claimed BUG-GPU-002 fix + its ARCH OK), fresh port 8922 (stopped after, verified no LISTENING socket remains), ANGLE Intel 0x7D45 D3D11.

- [ ] **`?gpucompare=1`: 7/7 PASS, holes = 0 on every row — NOT MET.** Reproducible on 3 consecutive loads (initial + 2 reloads, incl. one with a cache-busting query param): **5/7 PASS.** `test_room: start pose` now FAILs too (**holes = 1**, previously PASS before this fix) and `test_room: facing stair + 1.0m platform` FAILs **worse than before** (**holes = 5**, was holes = 1 pre-fix). Confirmed via `window.__gpuCompare` (`cmpGeom.holes: 1` / `5`, `pass: false` on both rows), not just the on-screen text. The other 5 rows PASS, including both target fixes:
  - [x] **BUG-CPU-001 fixed**: `world_m1 player spawn` row now PASS (holes 0, glyph 100%) — confirmed.
  - [x] **owner pose (1500.69, 1027.36, 3.00; yaw 236 pitch -29)**: gpucompare row PASS (holes 0, glyph 100%, matches `?force2d=1`/CPU) — confirmed. Also spot-checked visually: `?force2d=1` (backend `c2d-capped`, `path: cpu`) renders walls, consistent with the GPU path.
  - [x] Grid switch: `?grid=160x60` renders correctly (240x90 default also fine, confirmed via default `index.html?debug=1`). Note: `?gpucompare=1` always runs its internal compare at a fixed 160x60 regardless of a `?grid=` param on the URL — same as before, not a regression, just noting it isn't the 240x90 default.
  - [x] `?sprite=1` (`level=test_room`): brazier + lanterns render, overlay `sprites: 3`, no errors.
  - [x] No console errors on any page tested this session (default, grid variants, sprite=1, force2d=1, gpucompare=1).
  - [x] All Node suites green: `check-deps.mjs` (88 files), `check-deps.test.mjs` (8/8), `physics` (187/187), `jump` (111/111), `eyeFeel` (18/18), `playerLook` (10/10), `gpuCompare` (35/35), `WorldTextures` (12/12), `sprites` (49/49), `world` (20/20), `ShadeTextures` (1104/1104), `compositor` (10/10), `glsl` (33/33), `behaviours` (9/9), `handle` (15/15), `packed` (5/5), `serialize` (9/9), `terrain` (19/19), `tower` (49/49).

Bugs:
- **BUG-GPU-002 [major] reopened — `?gpucompare=1` holes=0 gate still not met, and the fix regressed the start-pose row**
  Steps: 1. Serve worktree @ a827b2c, open `game/index.html?gpucompare=1`. 2. Read `test_room: start pose` and `test_room: facing stair + 1.0m platform` rows. Reload 2-3x.
  Expected: holes = 0 on all 7 rows (per the programmer's 980a405 claim and the architect's ARCH OK of it).
  Actual: `start pose` now FAILs with holes = 1 (it PASSed before this fix — this row was not previously affected by BUG-GPU-002). `facing stair` FAILs with holes = 5 (worse than the pre-fix holes = 1). Both stable across reloads (not the earlier readback-timing flakiness). Recommend ASK ARCHITECT: the descending-step-riser / inclusive-WALL-boundary change in `dda.frag.js` (commit 980a405) appears to have introduced a new hole on the start pose and enlarged the hole count on the stair pose rather than closing them.
  This is geometry-only (`compareGeometry`), unrelated to sprites — does not change the US-030c assessment beyond "still blocked on US-030a".

Performance: not re-measured in detail this session (functional focus per instructions); `?debug=1` default view showed `gpu` in the low-single-digit ms range, consistent with prior runs.

Console errors: none.

**Verdict: US-030a stays FAIL / back to `dev`.** BUG-CPU-001 is confirmed fixed; BUG-GPU-002 is confirmed NOT fixed (reopened, and worse than the originally reported instance). US-030c remains blocked on US-030a per its own AC.
