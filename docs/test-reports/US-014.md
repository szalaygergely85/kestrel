# Test report US-014 – Lever opens the grate (Node-level)

Result: PASS

Scope: PO OK is Node-level only; visual/E-prompt ACs deferred to US-011/US-012.

Criteria (tester checklist from backlog):
- [x] `node engine/world/sectorAnim.test.js` – 85 passed, 0 failed.
- [x] `node game/js/quest/tower.test.js` – 63 passed, 0 failed (incl. `lever.pull` via `world.fireInteraction`).
- [x] `node tools/check-deps.mjs` – 4 findings, all in `tools/bench-voxel.mjs` (US-039, in progress); no findings on US-014 files. Ignored per instructions.
- [x] `stepSectorAnims` called once per world-mode frame in `game/js/main.js`, before `integrate` (lines 275-276).
- [x] Save-mid-open/load/finish determinism test passes (part of `sectorAnim.test.js` 85/85).
- [x] `design/levels/world_m1.js` no longer has the redundant `'tower.grate.open'` state key; comment confirms `structure.dynamics['grate']` is the only truth (lines 46-48).

Supporting green (round-trip / versioning):
- `node engine/world/serialize.test.js` – 9/9
- `node engine/world/packed.test.js` – 5/5
- `node engine/world/world.test.js` – 20/20

Bugs: none.

Performance: N/A (Node-level tests only, no browser run).

Console errors: N/A. Note: repeated stderr line `[World] 5 behaviour(s) referenced by level data but not registered: beacon.light, hint.show, lantern.take, lever.pull, quest.end` appears in every test run — pre-existing stub-registration warning from other in-progress stories (US-011/US-012/US-017/US-020/US-025 area), not caused by US-014 and not a failure.

Suggestions (non-blocking): none.
