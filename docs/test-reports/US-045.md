# Test report US-045 – WebGL2-required screen + software-renderer warning

Result: PASS

Criteria:
- [x] No WebGL2 support -> full-screen "WebGL2 required to play" message, no crash, no console error, no game loop underneath. Verified via `?webgl2gate=none`: page text shows the message only, console clean, canvas not rendering.
- [x] Software renderer detected -> game starts on GPU path, dismissible warning "Performance may be poor on this device" shown top-center, logged once (`[warn] [webgl2Gate] software renderer detected ((test override) SwiftShader) - performance may be poor`), no other behaviour change. Verified via `?webgl2gate=software`; exactly one console.warn seen, game rendered and ran normally.
- [x] `?gpu=0` unaffected as dev/debug switch, and precedence confirmed: `?webgl2gate=none&gpu=0` skips the gate entirely and runs the game normally (no full-screen message, no console errors).
- [x] Works the same at 240x90 and 320x120 (`?grid=240x90` / `?grid=320x120`); F3 debug overlay (top-left) and the software-warning banner (top-center) do not overlap at either resolution.

Bugs: none found.

Performance: fps overlay showed ~13-35 fps under the software-renderer test override at 320x120/240x90 (expected — SwiftShader override plus screen recording overhead, not representative of the real GPU path); no stutters or errors noticed.

Console errors: none in any tested mode.

Suggestions (non-blocking): could not exercise the pause menu together with the warning banner (banner auto-dismisses in 6s and pointer-lock/pause didn't trigger reliably under automation); a human owner check at 240x90/320x120 with pause menu open is still worth a quick eyeball per programmer notes, though risk is low since the overlay is viewport-anchored.

Node suites: `node tools/check-deps.mjs` OK (109 files, no violations); `node tools/check-deps.test.mjs` 8/8 fixture checks pass; `physics.test.js` 187/187; `jump.test.js` 111/111; `eyeFeel.test.js` 18/18; `playerLook.test.js` 10/10. All green.
