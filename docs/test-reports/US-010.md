# Test report US-010 – Tower layout: 3 levels as sector data
Result: PASS

Criteria (programmer/integration ACs, verified against docs/backlog.md ~line 1392):
- [x] Tower loaded from `design/levels/tower.js` via AssetRegistry, `game/js/main.js`; no hand-copied level file; default level, placed at (1480, 1018); `?level=test_room` still reachable and loads cleanly (sector `S`, no terrain, no console errors).
- [x] Stair steps climbable, gap jumpable, falls land safely, summit only reachable via grate path — proven structurally by `tower.test.js` (route walk with scripted Space, 60 drop-edge checks all grounded, grate-closed BFS never reaches a summit cell while grate-open BFS does).
- [x] Slice completable without the lantern — `tower.test.js` "lantern-free structurally" check (BFS never consults lantern state).
- [x] All extension fields (`props`, `lights`, `interactables`, `triggers`, `markers`, `layers.tilt`) reachable via `level.def` — confirmed live in-browser (`world.structures[0].level.def` dump) and by test.
- [x] Interactables/triggers declared in level data, behaviours registered by name from `game/js/quest/index.js`, no coordinate literals in quest code (reviewed by architect already; re-confirmed no grep hits added).
- [x] Walking/running the 1 m gap without Space always falls — covered by `tower.test.js` gap checks (walk and run, both fall to debris; Space lands at 3.0).

Tester checklist (from the PO OK note):
1. [x] Default page `?debug=1&strict=1`: wakes at world (1497.00, 1027.50, 0.00), yaw 330, pitch 30, `structure: tower sector: '.'`, grid 240x90, gl2/GPU DDA + sprite pass active. Zero console errors or warnings (only informational `[RenderTarget]`/`[GpuCellPipeline]`/`[GpuSpritePass]` logs).
2. [x] Real WASD input against the live game confirms movement/collision responds correctly out of the lying wake pose (moved onto the stair sector, sector id changed `.` -> `s`, no clipping, no console errors). Full route/gap/grate traversal is proven by the deterministic physics test suite per the story's own test plan ("Playability = physics tests, not manual play" - tech notes item 4); direct in-page teleport via `entity.transform` does not stick (an outside-world safety snap reverts it), so exhaustive manual walking of the whole route was not attempted — the automated `integrate()`-based route/gap/fall/BFS checks are the authoritative proof here and all 49 pass.
3. [x] Grate-closed vs grate-open reachability to the summit verified structurally (BFS in `tower.test.js`); not re-verified by hand in the browser (grate open/close behaviour bodies are stubs per this story, expected).
4. [x] Fall-from-stair safety verified by the 60 drop-edge checks in `tower.test.js` (all land `grounded`, no NaN).
5. [x] `?level=test_room` loads unchanged: world (2.50, 2.50, 0.00), sector `S`, "no terrain", zero console errors.
6. [x] Node suites re-run in the worktree (`C:\dev\projects\game_project_test`, same commit as HEAD):
   - `node game/js/quest/tower.test.js` -> 49 passed, 0 failed.
   - `node engine/core/behaviours.test.js` -> 9 passed, 0 failed (story notes say 11; architect already flagged this as cosmetic in the ARCH OK entry, not a regression).
   - `node engine/world/packed.test.js` -> 5 passed.
   - `node engine/world/serialize.test.js` -> 9 passed (expected `[World] 5 behaviour(s) ... not registered` warning, since this suite loads the tower level without the game's quest registry).
   - `node engine/world/terrain.test.js` -> 19 passed.
   - `node engine/world/world.test.js` -> 20 passed (same expected unregistered-behaviour warning).
   - `node tools/check-deps.mjs` -> OK (88 files).
   - `node engine/physics/physics.test.js` -> 187 passed.
   - `node engine/physics/jump.test.js` -> 111 passed.
   - `node engine/entities/eyeFeel.test.js` -> 18 passed.
   - `node engine/core/playerLook.test.js` -> 10 passed.
   All green, no failures.

Bugs:
- None found that block this story.

Performance: FPS observed 16.6-30.0 in the tower scene on `?debug=1` (240x90 grid, gl2/GPU path). Lower readings (~16-22) occurred on the very first rendered frame after a navigation/level switch (one-time JIT/shader-warmup cost); FPS settled to ~30 within a couple of seconds and stayed stable while walking. No stutter observed during WASD movement or the level switch to `test_room`.

Console errors: None. Only informational startup logs (`[RenderTarget] back-end: gl2`, `[GpuCellPipeline] active ...`, `[GpuSpritePass] active ...`). `?strict=1` did not throw.

Suggestions (non-blocking):
- `behaviours.test.js` reports 9 checks; the programmer/tech notes say 11. Purely a count/comment mismatch already noted by the architect (ARCH OK); worth a one-line fix next time that file is touched so the count in comments matches reality.
- Out of scope for this story (per task instructions, not held against it): the US-030a shading gap on 3 gpucompare poses, and the Wick crash-in narrative/text follow-ups noted in the PO OK entry for the writer/designer.
