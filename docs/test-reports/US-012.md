# Test report US-012 – Interaction system + brass lamp pickup

Result: PASS

Tested in `../game_project_test` @ dced4a2 (fresh server, port 8947, stopped afterward; port 8000 untouched).

Criteria:
- [x] Interactables (position, radius, prompt, `onInteract`) targeted within 1.8 m / ~20 deg of view centre, nearest-to-centre wins. Verified via `engine/world/interaction.test.js` (21/21) and live in-browser: brute-forced eye pose to face the lamp (dist 1.30 m, angle 7.5 deg) -> `world.interaction.prompt === '[E] Take lamp'`; turning ~30 deg off-cone or stepping back past 1.8 m both cleared the target/prompt; returning to range/facing restored it.
- [x] Crosshair dim by default / prompt appears under it when a target is active. Confirmed via `world.interaction.prompt` toggling correctly on enter/exit of range and cone (data-level); a manual render pass (`loop.render()`) drew the dark prompt plate with white text over the crosshair with no exceptions. Final gold/dim styling is explicitly deferred art (US-015 `uiStyle`), consistent with PO notes.
- [x] E on the lamp attaches the light and removes the hook sprite association. Verified: after a simulated `E` edge, `player.getComponent('light')` = `{preset:'lantern', on:true, attach:'eye', offset:{right:0.3,down:0.3,fwd:0.4}, sway:{amp:0.02}}` exactly per spec; `world.state['tower.lantern.taken'] = true`; used-flag `used.tower.lantern = true`. (Visual hook->empty sprite swap is deferred to US-011 per PO notes; not independently re-checked here.)
- [x] One pickup only, no drop. A second `E` press after pickup fires nothing (`prompt` stays `''`, `targetKey` stays `null`); `tower.lantern.taken` remains `true`; re-approaching the lamp afterward shows no target.
- [x] Engine generic / game-specific split. Code inspection confirms `engine/world/interaction.js` has no lamp/lever-specific logic; `game/js/quest/lantern.js` holds `lantern.take`; data-driven via `def.interactables` in `design/levels/tower.js`.
- [x] Lever (`E`) still opens the grate (US-014 visual check deferred here). Brute-forced a valid standing spot on the upper landing (floor z=3, pitch -32, yaw 0, angle 0.17 deg) -> prompt `[E] Pull lever`; `E` fired `lever.pull`, `tower.lever.pulled = true`, and `world.structures[tower].dynamics.grate.t` animated from 0 -> 0.73 (after 1.5 s of ticks) -> 1.0 (after 2.5 s), a genuine eased animation over real frames, not an instant snap.
- [x] Slice completable without the lamp: lever/grate path worked independently of lantern state; ambient-only readability re-check is deferred to US-006 per PO notes (not blocking here).
- Deferred by PO precedent, not re-checked here: AC3 render-side (glow, sway, ±5% flicker visible) and AC4 (3-step readability) — both need US-006's `syncEntityLights`, which doesn't exist yet.

Node tests (worktree, dced4a2): `interaction.test.js` 21/21, `attach.test.js` 10/10 (both `--expose-gc`, zero heap growth), `tower.test.js` 70/70, `check-deps.mjs` OK (106 files), `physics.test.js` 187/187, `jump.test.js` 111/111, `eyeFeel.test.js` 18/18, `playerLook.test.js` 10/10 — all green.

Bugs: none found.

Performance: manual 120-tick update+render burst averaged 0.38 ms/frame (well under budget); overlay showed frame ~0.3-0.4 ms, gpu ~3.8 ms during spot checks. No stutters observed across ~275 simulated frames (targeting sweeps, pickup, lever pull, grate animation).

Console errors: none, before or after the full test pass (`read_console_messages` clean throughout, including after the perf burst).

Notes on method: the automated Browser pane's `requestAnimationFrame` did not tick while the tab was backgrounded/unfocused (matches the programmer's note in their test pass), so live checks were driven by calling `window.__debug.loop.update(dt)` / `.render()` directly from the console — this exercises the exact same `update()`/`updateInteraction()`/render code path main.js's rAF loop calls, just invoked manually instead of waiting on a throttled rAF. Player pose was set via `playerHandle.data.transform` + `window.__debug.look.{yawDeg,pitchDeg}` (note: `look.pitchDeg` is clamped to +/-35 by `PlayerLook`, same as real mouse/arrow-key input).

Suggestions (non-blocking): none.
