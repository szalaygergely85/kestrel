# Test report US-025 – World model: terrain + placed structures in one world frame (D-007)

Result: PASS

Tested at commit `d7723fb` in the clean worktree `C:\dev\projects\game_project_test`, served on a fresh port 8793 (stopped after use; port 8000 never touched). No `git stash` used.

## Node tests / tooling
- `node tools/check-deps.mjs` → `check-deps OK (65 files)`.
- `node tools/check-deps.test.mjs` → fixture self-test 8/8 pass (its 6 "FAILED" lines are the intentional bad-fixture findings the self-test asserts on).
- `engine/world/{terrain,world,packed,serialize}.test.js`, `engine/entities/handle.test.js`, `engine/render/compositor.test.js`, `engine/physics/{physics,jump,eyeFeel}.test.js`, `engine/core/playerLook.test.js` → all `ALL PASS` (187/187, 111/111, 18/18, 10/10 ALL PASS, etc.), zero failures across the board.
- `node --expose-gc tools/bench-cast.mjs --gc` → every embedded checksum (v1 fast, v1 reference, v2) matched `OK`, including the `?shadetest`-style checks and the flicker metric (all `OK`). The only failing lines are the pre-existing, already-flagged US-028a "v2 total over budget" timing findings in files this story doesn't touch (not a regression from this story).

## Criteria (tester checklist from the PO OK note)
1. [x] **Far bake timing** – Per-frame JS execution time (isolated from the automation harness's own frame-scheduling gaps, by timing just the callback body): max observed 8.3 ms and 24.5 ms across two runs, both under the 25 ms/frame budget. Overlay reached `farReady true (100%)` with only sky/haze visible in the open span beforehand, and no console errors at any point. **Caveat:** wall-clock time-to-`farReady` could not be measured reliably through this browser-automation harness — it throttles the tab's `requestAnimationFrame`/timers heavily when not being actively driven (observed fps as low as 2.5, vs. the in-game 60 fps target, and gaps up to ~2 s between serviced frames), which inflated the *real* elapsed time to completion well past 5 s in every attempt. That is a harness artifact (confirmed by isolating actual JS execution time per callback, which stayed under budget throughout), not evidence of an engine regression. Recommend the PO/architect spot-check wall-clock bake time once in a normal, non-automated browser tab if a stricter confirmation is wanted.
2. [x] **Spawn pose + overlay** – World spawn at (1497, 1027.5, 0), matching `def.start` offset by tower origin (1480, 1018). Overlay showed world x/y/z, `structure: tower`, `sector: '.'`, `farReady`/bake %, chunk cx,cy — all present and correct.
3. [x] **Walking + jumping in the tower** – Held `KeyW`: player moved ~6.4 m (1497,1027.5)→(1493.3,1022.3) with no console errors. `Space` jump: vz started at 6.17 and decayed under gravity, peak z=0.70, landed cleanly back to `grounded:true, vz:0`. Feel matches US-008/009. No console errors during either.
4. [x] **Serialize round-trip** – Via `engine/index.js`'s `serialize`/`deserialize`: opened the grate (`world.animateSector('grate', 0.5)`), then `serialize → JSON.stringify → JSON.parse → deserialize → serialize` gave a deep-equal result (`JSON.stringify(s1) === JSON.stringify(s2)`), included grate dynamics and `nextId`, and a recursive scan found zero keys starting with `_` in either serialized object.
5. [x] **`?level=test_room` + `?shadetest=1`** – `test_room` renders correctly, overlay shows world coords (2.50, 2.50, 0.00), `structure: test_room`, `sector: 'S'`, "no terrain", zero console errors. `?shadetest=1`: `361/361 pass` (US-004b) and `v2: 1954/1954 pass` (US-028), both `ALL PASS`, zero console errors.
6. [~] **`grep -r "tower\|1480" engine/`** – Does **not** come back empty; it matches in `engine/core/assets.js`, `engine/core/behaviours.js`, `engine/entities/Player.js` (all comments), `engine/world/MAP_FORMAT.md` (documentation), and five `*.test.js` files (`packed`, `serialize`, `world`, `terrain`, `compositor`) that deliberately import `design/levels/tower.js` / use coordinate 1480 to exercise the engine against real content, per the story's own reviewed build/test plan. No production/runtime engine file (`World.js`, `Terrain.js`, `compositor.js`, `packed.js`, `sectorCaster.js`, `integrate.js`) hard-codes the tower or its coordinates outside test fixtures — the underlying AC intent ("only `game/js/main.js` knows the tower or its coordinates") holds. Logged as BUG-1 below since the literal grep specified in the checklist does not pass; judged non-blocking.

## Bugs
- BUG-1 [minor] Tester checklist item 6 (`grep -r "tower|1480" engine/` finds nothing) fails literally.
  Steps: 1. `cd engine && grep -rn "tower\|1480" .`
  Expected: no output.
  Actual: hits in 3 source-file comments, `MAP_FORMAT.md`, and 5 test files that import `design/levels/tower.js` (by design, reviewed in the story's build order/test plan).
  Not a functional defect — production code paths remain fully generic — but the checklist wording and the actual accepted test strategy are inconsistent. Suggest the PO/architect either scope the grep to non-test/non-doc files going forward, or note the exception explicitly in the AC.

## Performance
- 60 fps / ~5 ms frame time observed in normal play (overlay), consistent before and after bake completion.
- `bench-cast` checksums unchanged from the US-028 baseline; only the pre-existing, already-flagged v2-over-budget timing finding remains (not this story's regression).
- Machine was noisy (~60% background CPU per user note) and the browser-automation harness itself throttled backgrounded-tab timers/rAF significantly (observed fps down to ~2.5) independent of CPU noise — factored into judging criterion 1 above rather than treated as a game-performance regression.

## Console errors
None observed across `world_m1`, `?level=test_room`, or `?shadetest=1`. Only expected `[RenderTarget] back-end: gl2` info logs and the shadetest's own pass logs.

## Suggestions (non-blocking)
- Consider excluding `*.test.js` and `*.md` from the "no tower/1480 under engine/" check (or wording it precisely) to avoid the literal/intent mismatch found in BUG-1.
- A future story could expose a lightweight, harness-friendly bake-timing hook (e.g. `window.__debug.world.terrain._bakeStats = {startedAt, readyAt}` in real wall-clock `Date.now()`) so automated testers can measure wall-clock bake time without fighting `requestAnimationFrame` throttling quirks in headless/automated tabs.
