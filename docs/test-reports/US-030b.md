# Test report US-030b – N-ray coverage anti-shimmer + flicker metric + owner walk-test

Result: PASS

Tested in worktree `../game_project_test` @ 9970528, fresh port 8951 (stopped after), owner's 8000 untouched. Browser: ANGLE (Intel, 0x7D45) D3D11.

Criteria:
- [x] N-ray coverage anti-shimmer built/wired — confirmed via `rays: 2` on default `?debug=1`, renders correctly.
- [x] `?rays=1` still works — renders correctly, rays: 1 shown, no console errors.
- [x] `?gpucompare=1` (default, forces rays=1) — 7/7 PASS, kind 100%, glyph 100%, holes 0, poisonedSurvivors 0 on every pose.
- [x] `?gpucompare=1&rays=2` info row — kind 100.00% on all 7 poses (>=99.5% required), holes 0, glyph 82-98% (reported only, not gated), does not affect ALL PASS.
- [x] Flicker `totalPct` AC — PO-waived 2026-09-24 (harness warm-up outlier); not re-litigated per instructions. Follow-up US-044 tracks the harness fix.
- [x] Owner walk-test — already PASSED (2026-09-23, owner's Chrome, port 8930); this is the story's actual acceptance gate per PO ruling.
- [x] No console errors on any of the above pages.
- [x] Frame time not regressed — default page renders and stabilizes (fps rising to ~24 in this sandboxed Browser pane after warm-up); GPU/JS ms budgets were already measured on dev/owner hardware in the story notes, not re-measured here (this environment's software-rendered GPU timer is not representative).
- [x] Node suites: `check-deps.mjs` OK (106 files), `check-deps.test.mjs` 8/8 (fixture), `flicker.test.js` 9/9, `glsl.test.js` 33/33, `gpuCompare.test.js` 35/35, `physics.test.js` 187/187, `jump.test.js` 111/111, `eyeFeel.test.js` 18/18, `playerLook.test.js` 10/10 — all green.

Bugs: none found.

Performance: Browser-pane sandbox only (not representative of real hardware); no stutters observed during default/`rays=1` page loads. Real budget numbers already captured by the programmer/owner in the story's own notes.

Console errors: none on default, `?rays=1`, `?gpucompare=1`, `?gpucompare=1&rays=2`.

Suggestions (non-blocking): none new; US-044 (flicker harness fix) already queued.
