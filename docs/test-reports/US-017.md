# Test report US-017 – End trigger, fade and restart

Result: **FAIL**

Tested at commit `41bfdc6` in the worktree `C:\dev\projects\game_project_test`, own server on port 8991 (stopped after; port 8000 untouched). Real Chrome (via the browser tool), `game/index.html?debug=1`. Pointer lock does not engage in this automation, so the live walk/trigger/fade/restart sequence was driven by real `keydown`/`keyup` `KeyboardEvent`s plus manual `loop.update(1/60)` ticking through `window.__debug` (rAF is fully frozen while the pane is backgrounded here, confirmed via a frozen `loop._rafId`/`_lastTime` over real wall-clock waits — the same headless-throttling artifact the programmer already flagged). This still exercises the real production `update()`/`render()` closures in `main.js`, not a stubbed path.

## Criteria
- [x] Entering the outcrop trigger cells locks input; camera walks ~1 m/1.5 s and pitches to -12° and **stays there** (no snap-back) — verified live, twice (before and after a restart).
- [x] Screen fades to black over 2 s via the glyph ramp, **on the default GPU path** — confirmed both visually (pure black frame after fade) and via `?gpucompare=1` (`sceneFade=0.5` pose: `kind=100.00% glyph=100.00%`, `ALL PASS`, includes sprites).
- [ ] Screen fades to black over 2 s **on `?gpu=0`** — **FAILS, see BUG-1**.
- [x] End-card text, order and cursor: `The signal is still calling.` / `Someone is out there.` / (1.5 s gap) `- to be continued -` / `[R] Wake again_`, read back byte-exact from `rt.cells` on the GPU path.
- [x] R restarts from the wake sequence with state reset: verified live — `tower.lantern.taken`, `tower.lever.pulled`, `hints.shown`, `ui.mapCard.shown` and `quest.endT` all round-tripped back to their initial values, and the player transform returned to the exact spawn pose (1497, 1027.5, 0, yaw 330, pitch 30). `R` is correctly gated by `computeEndCardState(...).canRestart`, confirmed by source read (`main.js` line 442-445) — it cannot fire before `[R]` is shown.
- [x] A second full run to the end (after restart) reaches the trigger and completes the walk/fade/text sequence again cleanly, no console errors.
- [x] Trigger/behaviour registration (`def.triggers` entry `end`, `quest.end` from `game/js/quest/`) — confirmed via `window.__debug.world.triggers[0]` (`id:'end'`, cells `[[5,5],[5,6],[5,7],[5,8]]`, `walkTo`, `pitchTo`, `trigger:'quest.end'`).
- [x] Boulder/lever/lamp/grate restart correctness — not independently re-verified in the browser (no accessible boulder/grate entity via `window.__debug`), but this is the exact scenario `game/js/quest/restart.test.js` covers against real `world_m1` (take lamp, pull lever, mid-open grate, push boulder, fire `quest.end`, then `serialize(deserialize(initialState))` deep-equals `initialState`) — 16/16 checks pass.

## Bugs
- **BUG-1 [major] `?gpu=0` never visibly fades the 3D scene or sky**
  Steps: 1. Load `game/index.html?gpu=0&debug=1`. 2. Force/walk into the `end` trigger and let `quest.endT` run well past the 2 s fade (confirmed at `endT` up to 5-7 s). 3. Inspect the rendered frame / `rt.cells`.
  Expected: sky and terrain glyphs/colors step down the fade ramp toward black over 2 s, same as the GPU path (AC2: "fades to black... for both the 3D view and the text").
  Actual: sky cells stay exactly `bg=[196,220,239]` (unfaded light blue) and terrain glyphs keep their non-space characters indefinitely; nothing in the 3D view fades. Root cause: `engine/render/CellBuffer.js`'s `clear()` and `setCellRGB()` (the CPU raycaster's normal per-frame drawing path) set `cb.mask[i] = 1` on every cell, and `applySceneFade` (`engine/ui/fade.js` line 78: `if (mask[i]) continue;`) skips every masked cell — so on `?gpu=0` **all** cells are treated as "JS-owned, don't touch" and the fade is a full no-op for the 3D view. This is the same mask-conflation the programmer already found and worked around for the `?gpucompare=1` oracle (notes item 3: "clearing the mask... harmless in real gameplay, the GPU-DDA path never runs this CPU cast") — but `?gpu=0` **does** run this same CPU cast for real, on-screen rendering, so the workaround wasn't applied where it's actually needed. The default/shipping GPU path is unaffected (its own pass-F fade doesn't use `cb.mask`) and is verified working.
  Not blocking M1 shipping (per D-017, `?gpu=0` is a dev/debug switch only, no perf AC, and it isn't the path that ships), but it is an explicit tester-notes/AC line item ("both on the default GPU path and on `?gpu=0`") that currently fails, and the story's own PO-OK claims CPU fade was verified.

- **BUG-2 [minor, dev-tooling only] `window.__debug.world/playerHandle/look` go stale after a restart**
  Steps: 1. Reach the end, press `R`. 2. Read `window.__debug.look` / `window.__debug.world`.
  Expected: reflects the new world/look created by the `'world:loaded'` rebuild.
  Actual: `window.__debug.loop/world/playerHandle/look` are set once (`main.js` ~line 578-582), right after the *first* `engine.run(...)`, and never refreshed on restart; the live `look`/`world`/`playerHandle` used by the real per-frame closures are reassigned locally and diverge from the stale `window.__debug` copies (confirmed: setting `window.__debug.look.yawDeg` had no effect on player movement post-restart; `window.__debug.engine.world` is correct and can be used as a workaround). Does not affect gameplay or the F3 overlay (which reads the live closure variables directly), only external tooling reading `window.__debug` post-restart. Worth a one-line fix (re-assign these four fields inside the `'world:loaded'` listener) since it's the same hook the tester/future debugging relies on.

Performance: not benchmarked (US-018's job); GPU path showed 20-35 fps in this throttled/backgrounded automation pane, not representative of real hardware.

Console errors: none observed in any run (default GPU, `?gpu=0`, `?gpucompare=1`, restart, second run).

Suggestions (non-blocking): none beyond the two bugs above.

## Disposition
FAIL → story status set back to `dev`. Bugs for the programmer: BUG-1 (major, `?gpu=0` scene fade no-op via the `cb.mask` conflation) and BUG-2 (minor, stale `window.__debug` refs after restart). Everything else (trigger firing, walk/pitch lock-and-hold, GPU fade incl. sprites, end-card text/timing/cursor, restart state reset incl. a verified second run, `R` gating) checks out.

## Programmer fix pass 2 (2026-09-24)
- **BUG-1 fixed.** Root cause confirmed as diagnosed above: on the CPU path (`?gpu=0`, and the `?gpucompare=1` JS oracle) the raycaster/terrain/sky/sprites all draw through `CellBuffer.setCellRGB`, which sets `mask=1` on every cell it touches - the same flag real UI overlay writes use. By the time any caller reaches `applySceneFade` (always after the 3D scene, always before any UI is drawn that frame), every cell is masked and the fade is a full no-op. The `?gpucompare=1` oracle already had a one-off `mask.fill(0)` before its own `applySceneFade` call to work around exactly this; the real `render()` path in `main.js` didn't. Added `clearMaskForSceneFade(rt)` (`engine/ui/fade.js`, exported from `engine/index.js`) and call it right before every CPU-path `applySceneFade` - the real gameplay call site (`game/js/main.js` `render()`) and the `?gpucompare=1` oracle (now sharing the same helper instead of its own inline workaround/comment). Added a unit test (`engine/ui/fade.test.js`, section 9) that reproduces the BUG-1 scenario (a `setCellRGB`-drawn scene cell, mask=1) and confirms `applySceneFade` is a no-op without the helper and fades correctly with it.
- **BUG-2 fixed.** `window.__debug.world/playerHandle/look` are now (re)assigned inside the `'world:loaded'` event handler itself (`game/js/main.js`), the single place both the first load and every restart (`R`) go through, instead of only once right after the first `engine.run(...)` call.
- **Verification.**
  - Node: `engine/ui/fade.test.js` (19/19, incl. the new BUG-1 test), `engine/physics/physics.test.js` (187/187), `engine/physics/jump.test.js` (111/111), `engine/entities/eyeFeel.test.js` (18/18), `engine/core/playerLook.test.js` (10/10), `game/js/quest/restart.test.js` (16/16), `engine/render/gpu/sprites.test.js` (55/55), `node tools/check-deps.mjs` (OK, 114 files) and `node tools/check-deps.test.mjs` (8/8) - all pass.
  - Browser: own server on port 8993 (stopped after; port 8000 untouched), real Chrome via the browser tool, `game/index.html?gpu=0&debug=1&level=test_room`. Forced `world.state['quest.endT']` via `window.__debug.world` and called `window.__debug.loop.render(0)` directly (rAF is frozen while this pane is backgrounded, same as the original tester note) to get before/after frames: an unfaded sky cell read `bg=[141,183,227]`, fully faded read `bg=[0,0,0]`; a 3D-scene glyph cell went from glyph 13 to glyph 0 (space) once faded, and back to a real glyph when un-faded again - the CPU-path scene now visibly fades. `game/index.html?gpucompare=1` stayed `ALL PASS` (8/8 poses, including the `world_m1: player spawn, sceneFade=0.5` pose, geometry 100%/shading 100%).
- **Files changed:** `engine/ui/fade.js` (new `clearMaskForSceneFade` export, the fade mask check only - no other engine/render code touched), `engine/index.js` (export it), `game/js/main.js` (call the helper at both CPU-path `applySceneFade` call sites; refresh `window.__debug` refs in the `'world:loaded'` handler), `engine/ui/fade.test.js` (new BUG-1 regression test), `docs/backlog.md` (status -> `testing`).
- Status (header + row) -> `testing`: the change is game-side (`main.js`) plus the fade mask check itself in `engine/ui/fade.js` - no other GPU/engine render code was touched.
