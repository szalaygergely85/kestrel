# Test report US-016 – Far overworld view through the breach

Result: PASS

Tested at commit `ade34bd` (worktree `game_project_test`), own server port 9057 (stopped after, port 8000 untouched).

Criteria (per PO's tester notes, US-016 backlog):
- [x] Breach view (1486.5, 1025.0, eye 7.6, yaw 270) at 240x90/320x120: terrain fills the opening (rolling hills, forest, path/river valley, fog to horizon, no sky seam); signal tower visible as a dark notch on the ridge with a small light; turning to yaw 87.6 near the relay plinth (eye 8.2) shows Ferrum only through the narrow east-wall notch, over sky cells (structure otherwise blocks it), matching the design's "glimpse through the notch" intent. Visually confirmed via `window.__debug` teleports + screenshots (pointer lock does not engage in this automated pane, as expected/documented; camera placed directly via `__debug`).
- [x] Pitch sweep -35/0/+35 at the breach: clean horizon, no seam/step at any pitch (0 shows terrain-to-sky gradient with no jump; +35 is pure sky; -35 shows the interior floor/rock down to where terrain resumes, no gap).
- [x] `?gpu=0`: JS/CPU oracle path renders full colour (not black) — confirmed via the `terrainShade.test.js` BUG-OWN-004 regression (near terrain, b=0.9, no channel near-black) and a live mid-wake-sequence screenshot showing correctly lit, coloured interior geometry on the CPU path. No perf check on this path (D-017), none run.
- [x] `?terrain=0`: verified via `window.__debug.gbuf` readback — all 38,400 cells kind 0 (sky), zero kind-7 (terrain) cells, `gpuPipeline.terrainEnabled === false`, no console errors.
- [x] `?gpucompare=1&grid=320x120`: **20/20 ALL PASS** in-browser (all poses incl. `breach`, `breachDown`, `parapetSky`, `signal tower`, `summit east`, `breach looking back east`), no console errors.
- [x] Node suites: all 42 `*.test.js`/`*.test.mjs` files under `engine/`, `game/`, `tools/` run individually — all green. `node tools/check-deps.mjs` OK (131 files). `node tools/check-deps.test.mjs` ALL PASS (8/8, its own fixture suite). `node --expose-gc tools/bench-cast.mjs --gc`: only the pre-existing, unrelated "v2 total p50 vs 3.5 ms escalation trigger" line FAILs (tracked separately under US-028/US-004b; confirmed nothing in this story touches `sectorCaster.js`'s hot path) — matches every prior pass's own finding, not a regression.
- [~] JS (CPU) frame ms at the breach, 240x90/320x120: **not reliably measurable in this environment.** The automated pane cannot hold real pointer lock / a focused, driving rAF loop (same limitation documented across every prior pass of this story — `document.visibilityState`/pause-on-unlock freezes the fixed-step loop and `fps`/`frame ms` read 0.0 once paused). Not failed on this basis per instructions; left to the owner's real-hardware check alongside the existing GPU p95 to-do.

Bugs: none found.

Performance: GPU total ms and JS frame ms both need the owner's real-hardware, focused-tab, real-mouse check (already an outstanding to-do carried from the PO/architect notes) — this headless pane cannot produce a trustworthy reading for either. Terrain GPU submit cost was already measured by the programmer at ~0.6 ms p50 delta (`?terrain=1` vs `?terrain=0`), within the 1.0 ms budget line.

Console errors: none observed in any of the tested modes (`?gpucompare=1`, default, `?gpu=0`, `?terrain=0`).

Suggestions (non-blocking): none.

**Owner real-GPU check (2026-09-24, owner Chrome, 320x120):** breach with terrain gpu 3.25 ms, with `?terrain=0` gpu 2.67 ms -> terrain ~0.6 ms (<= 1.0 ms: PASS). Total GPU <= 4 ms: PASS. But 30 fps at the summit in both cases (spawn 60 fps) -> BUG-OWN-006.
