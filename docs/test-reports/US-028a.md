# Test report US-028a – Stable detail when moving (anti-swim)

Result: PASS

Tested in clean worktree `C:\dev\projects\game_project_test` at commit `1153413` (re-verified after being moved from stale `2c24f83`).

Criteria:
- [x] F1 hash keying (bix/course, coarser texel fallback) mirrored in `shadeDetailFast`/`shadeV2`/`util.shade` – `?shadetest=1` fresh tab/port: v1 361/361, v2 1954/1954, 0 console errors.
- [x] F2 `grid.maxCover` 0.25 re-export – reflected in bench/compare numbers below.
- [x] Bench flicker metric implemented and green: fwd 0.33 %, strafe 0.80 %, yaw 0.85 % (all ≤ 1.0 %); total avg 5.79 % (≤ 7 %); A-B-A 0.00 % (≤ 0.3 %).
- [x] Distinct-glyph gate: ≥ 10 on all poses except `long diagonal, pitch -35` = 9 (per-pose `minDistinct`), matches PO/architect ruling.
- [x] `node --expose-gc tools/bench-cast.mjs --gc`: ALL CHECKS PASS, 0 GC, heap 128–206 B/frame, fast-vs-reference 0 mismatches, v1/v2 checksums OK, extra p50 best-of-3: 0.910 ms (sky/low-wall), 0.852 ms (long diagonal), 0.841 ms (low-wall-sky) – all ≤ 1.0 ms budget (no stair pose hit this run; budget noted as noisy per owner, judged on best-of-N).
- [x] `node tools/compare-detail-export.mjs`: 99.47/99.49/100.00 on same-surface set (≥ 95/95/95).
- [x] `node tools/check-deps.mjs`: OK (65 files).
- [x] Engine unit tests: playerLook, eyeFeel, handle, jump, physics, compositor, packed, serialize, terrain, world, check-deps.test.mjs – all ALL PASS.
- [x] Game (`game/index.html?debug=1`, port 8792): loads with fps 60, no console errors. Simulated walk/strafe/turn near a wall/stair edge via synthetic keyboard events (single key, multi-key held together, rapid tap toggling) – no console errors, position updates correctly, grid detail stays crisp in the screenshot (no obvious swim artifact from a static check).

Bugs: none found.

Performance: bench best-of-3 p50s all within budget; fps 60 in-browser, frame ~4.1 ms, grid draw ~2.7 ms at 160x60.

Console errors: none, in `?shadetest=1` or the game.

Remaining manual check: owner to judge walking smoothness/shimmer subjectively in real Chrome (per US-029 gate note, this tester's synthetic-input pass cannot substitute for the owner's own feel-check).

Suggestions (non-blocking): none.
