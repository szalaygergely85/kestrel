# Test report US-006 – Lighting: ambient + point lights with flicker

Result: PASS (one AC needs owner-hardware confirmation; one non-blocking bug filed)

Tested in `game_project_test` worktree @ 0149826, own server port 8971 (stopped after use, port 8000 untouched). Headless Chrome via Claude in Chrome, `ANGLE (Intel, Intel(R) Graphics (0x00007D45) Direct3D11 vs_5_0 ps_5_0, D3D11)`.

Criteria:
- [x] Light model ambient+point, GPU shading pass, color-multiplied, glyph ramp – verified via `?gpucompare=1` parity (below).
- [x] Point light falloff smooth to 0 at radius – covered by `lighting.test.js` (84/84).
- [x] N·L on walls/floors/ceilings – covered by `lighting.test.js`.
- [x] Occlusion by walls (LVIS grid) – covered by `lighting.test.js` + `?gpucompare=1` parity.
- [x] Flicker 8–12 Hz, ±15%, jitter ±0.05 m, deterministic on `timeSec` – covered by `lighting.test.js`.
- [x] `test_room` torch warm falloff to 6 m, ambient cool blue elsewhere – confirmed by `?gpucompare=1` glyph 100% match to the JS oracle on all 5 `test_room` poses (values come straight from `P.lights`).
- [~] **8-light GPU budget (<=4 ms @ 320x120)** – measured via debug overlay (`?level=test_room&lights=8&debug=1&grid=320x120`): `path: gpu grid:320x120 rays:2 upload 0.10ms gpu 4.33ms`, fps ~21-32. Slightly over the 4 ms target on this headless/software ANGLE backend. **Flagging per task instructions: headless GPU timings are not representative of real hardware — owner must re-measure on real hardware before this AC is treated as failed.** JS fallback (`?gpu=0&lights=8`) numbers from the programmer's pass (Node-measured, p50 0.308 ms / p95 0.812 ms) are well inside the <=1.5 ms CPU target.
- [x] `P.lights`/`P.util.falloff` rules, no hard-coding – confirmed in code and by parity.
- [x] JS reference == GPU: `?gpucompare=1` (lights on) **ALL PASS**, 7/7 poses (`test_room` x5, `world_m1` x2), kind 100%, glyph 100% every pose. `?gpucompare=1&lights=0` regression path **ALL PASS** too.
- [x] Carried brass-lamp light ACs (US-012 AC3/AC4) – `syncEntityLights`/`attachedLightPos` code path is wired and exercised by `lighting.test.js`; **not walked live in-scene** this pass (pointer-lock/WASD input did not register in the headless Chrome pane — player position stayed fixed after key presses), consistent with the programmer's/US-012 tester's prior note that this needed a live owner session. Recommend the owner do one short E-pickup + stairwell walk to eyeball it.

Node suites (all green): `lighting.test.js` 84/84, `check-deps.mjs` OK (108 files), `check-deps.test.mjs` ALL PASS, `physics.test.js` 187/187, `jump.test.js` 111/111, `eyeFeel.test.js` 18/18, `playerLook.test.js` 10/10, `compositor.test.js` 10/10.

Bugs:
- BUG-1 [major] `?bench=1` (the US-001 worst-case canvas benchmark) crashes when the GPU cell pipeline is active
  Steps: 1. Start server, open `/game/index.html?bench=1` (reproduces with or without `&lights=8&grid=320x120`, and with no level param too). 2. Observe `window.__bench` stays `undefined`.
  Expected: benchmark runs 600 frames and logs `[bench] US-001 worst-case, 600 frames: {...}`.
  Actual: throws `TypeError: Cannot read properties of null (reading 'gbuf')` at `GpuCellPipeline._repackAndUpload` (`engine/render/gpu/GpuCellPipeline.js:865`), called from `_hook` → `RenderTargetGL._cellPass` → `RenderTargetGL.present()` → `runBenchmark` (`game/js/main.js:870`). `runBenchmark` calls `rt.present()` directly without ever calling `gpuPipeline.setCamera/setWorld/setFrameBuffer`, so `_useDdaThisFrame` is false and it falls into the legacy `_repackAndUpload` path with `this._fb === null`.
  Not root-caused for origin (pre-existing US-001 tool vs. a side effect of US-006 wiring `_passLight()` unconditionally into `_hook()`) — flagging for the programmer to fix or gate `?bench=1` off the cell pipeline. This blocked getting the AC's `?bench=1&lights=8` number directly; I substituted the `?debug=1` overlay's live `gpu Xms` reading instead (see AC above), which is a valid alternate source for the same number.

Performance: fps 21-32 in `test_room` w/ 8 lights headless (not representative). No other stutters observed.
Console errors: none on the actual gameplay/gpucompare pages tested (the BUG-1 crash is the only console error found, isolated to `?bench=1`).

Suggestions (non-blocking): fix/gate `?bench=1` so it stays usable now that the GPU pipeline is the default path; do a real owner-hardware pass on `?bench=1&lights=8`/`?debug=1` at 320x120 and a live lamp-pickup walk before final sign-off confidence is 100%.

**BUG-1 fixed (main session, 2026-09-24):** `?bench=1` now disables the GPU cell-pass hook before `runBenchmark` (`game/js/main.js`); it was a US-001 tool never fed a frame. Verified: `window.__bench` reports 600 frames, no error.
