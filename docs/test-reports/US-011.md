# Test report US-011 – Billboard props + prop art (D-011 reskin)

Result: PASS

Tested against worktree `../game_project_test` @ 180e97a, own server on port 8999 (stopped after). Browser: pointer lock does not engage headless (confirmed, `document.pointerLockElement` stays null after a real click) - driven via real `KeyboardEvent`s on `window` plus `window.__debug` (`playerHandle`, `look`, `loop._tick()`) to set camera pose and force fixed-step ticks, per the tester brief.

Criteria:
- [x] `?gpucompare=1` (own port 8999): 12/13 rows PASS. "world_m1: boulder mid-roll" FAILs (fgMax 102, outside 0.146%, 14 cells) - **known FAIL BUG-LIGHT-001**: geometry `kind 100%`/`matEq`/`planeEq` all 100%, `glyph 100%`, `holes 0` - every mismatching cell is a surface/shading cell, no sprite-cell mismatch, matching the documented signature exactly. Accepted per the PO/architect ruling.
- [x] At `?grid=160x60` (and checked at default 240x90 too): walked/teleported to the programmer's own compare poses - burner (embers/gauge visible, torch-lit), lamp + bracket, lever housing, relay (half LOD, distant), boulder (resting on floor, moss visible) - all render without holes; nothing looked like it blocked the wake -> burner -> stair path (unchanged from `pathCheck`).
- [x] **US-012:** pressing `E` on the brass lamp (in range 0.64 m, cone 16.75 deg < 20 deg, prompt `[E] Take lamp` shown) sets `sprite.anim = 'empty'`, `tower.lantern.taken = true`, `used.tower.lantern = true`, clears the prompt, and turns on the carried light (`components.light = {preset:'lantern', on:true, attach:'eye', ...}`). Pressing `E` again is a no-op (once-flag holds).
- [x] **US-013:** boulder visible at rest on the floor (no float/sink) in the compare pose; roll frames are driven by distance (Node `boulder.test.js` 29/29, `roller.test.js` covered by physics suite).
- [x] **US-014:** `E` near the lever (dist 0.96 m, angle 3.4 deg, prompt `[E] Pull lever`) plays the 5-frame pull clip (`frame` observed stepping 0 -> 1 -> 4, `playing` false at the end, ~0.4 s of fixed-step time), `tower.lever.pulled = true`, `used.tower.lever = true`, prompt clears; the grate's sector animation ran (`structVersion` bumped, dynamics processed and settled) - no further `E` action.
- [x] `?force2d=1` renders the world without error (screenshot confirmed, zero console errors). `?gpu=0` not separately re-checked but shares the same JS reference path exercised by `?gpucompare=1`.
- [~] Restart (`R`) restores lamp/lever/boulder/grate: **verified at the Node level only** (`game/js/quest/restart.test.js`, 16/16 PASS, exercises serialize/deserialize round-trip for all four). Not re-exercised in the browser because `R` is gated behind `ending` (US-017: only live once `[R] Wake again` shows, i.e. after walking the full end sequence) - out of scope to fully play through headless in this pass; the Node suite is the documented gate for this mechanism and it's green.
- [x] Zero console errors throughout (checked after gpucompare, after lamp/lever interaction, after force2d).

Bugs: none new. BUG-LIGHT-001 (known, already filed, P1, not a blocker) reconfirmed with the exact documented signature.

Performance: HUD-reported fps 25-60 (headless/software GL, `frame` 0.2-0.9 ms typical); not a representative perf number for the owner's machine, no stutter observed while stepping.

Console errors: none.

Suggestions (non-blocking): none.
