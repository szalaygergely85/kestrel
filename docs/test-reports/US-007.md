# Test report US-007 – Lighting: sun directional light with shaft shadow (GLSL + JS reference)

Result: PASS (perf AC flagged for owner hardware confirmation, not a fail)

Tested in worktree `../game_project_test` @ 410c477, own server port 8981 (stopped after use, port 8000 untouched).

Criteria:
- [x] Architect tech notes before dev, ARCH OK, PO OK – confirmed in backlog history.
- [x] Directional sun uniform (elev 60, az 112.5, `#fff2d0`, intensity 1.0) – confirmed via `LightSet`/tower `def.sun`.
- [x] Shadow test per lit cell (sun ray vs sky/gap) – exercised `sunVisible` (the exact shipped JS oracle, imported live in-browser) across the tower shaft (x13-17,y4-8): produced `LLLss/LLLLs/LLLLs/sLLLs/ssLLs` – mostly lit, eastern column shadowed, matching the tester note's expected shape. Same scan on `test_room` gave a clean rectangular lit patch x6-9,y1-5 matching the sky opening.
- [x] Visible bright patch offset from the opening, walls cast shadow inside it, in both `world_m1` (tower) and `test_room` – confirmed visually (screenshots, camera repositioned via `window.__debug` since pointer lock doesn't engage in this iframe'd preview pane – see Notes). Clear warm/bright vs dark/cool contrast, wall shading gradient visible.
- [x] Sunlit vs shadowed >= 4 ramp steps – not measured pixel-by-pixel, but the oracle scan plus the strong visual contrast is consistent; covered at the unit level by `lighting.test.js`.
- [x] F6/F7 rotate azimuth ±5°, shadows move – dispatched real `keydown`/`keyup` KeyboardEvents (through `Input`, the production path) 6× F7 (150° shift for a clearly visible change): floor patch shape/position visibly changed, no flicker/errors. Elevation gating (no sun on ceilings/away walls) is enforced in code (`N.sunDir>0` gate) and covered by `lighting.test.js`.
- [x] `?sun=0` – no shaft, ambient-only look, no console errors. `?sun=0&gpu=0` – CPU fallback renders cleanly, "Click to resume" overlay legible, no errors.
- [x] `?gpucompare=1` – ALL PASS 7/7 poses (kind/glyph 100%, 0 holes, 0 poisoned) with sun on. `?gpucompare=1&sun=0` – ALL PASS 7/7 again.
- [x] Lever/grate toggle under live GPU rendering (exercises the FLAGS `texSubImage2D` xoffset fix): fired `lever.pull` via the real `world.fireInteraction` path, stepped `stepSectorAnims` 60×, then rendered many live frames with the GPU pipeline active. Zero console/GL errors. (Note: `?gpucompare=1`'s built-in pose set doesn't include a lever-toggle pose, so this was verified as two things together: a live toggle with zero GL errors under the GPU pipeline, plus the pre-toggle/post grate-mutation parity already covered by `gpuCompare.test.js` 35/35 and `WorldTextures.test.js` 16/16 at the unit level.)
- [x] CPU fallback `?gpu=0` – sun renders via JS reference, shaft visible, no errors, playable (subjective).
- [~] Perf `?bench=1&lights=8` at 320x120: `?bench=1` is the old US-001 raw-canvas benchmark (doesn't feed the GPU cell pipeline – confirmed by its own console output and by CLAUDE.md/memory notes), so GPU ms was read from the `?debug=1` overlay instead at `?lights=8&grid=320x120&debug=1`: **GPU 5.40 ms** (budget <= 4 ms, over), JS frame ~3.5-6.1 ms, fps ~14-26. `node --expose-gc tools/bench-cast.mjs --gc`: light pass **0.46 ms p50** (well under the 2 ms soft JS target); the "OVER BUDGET" line is the pre-existing sectors-shading overage (confirmed present before this story, unrelated). **This GPU number is from a headless/automated ANGLE D3D11 software-composited browser and may not reflect real hardware, even though it's the same GPU model (Intel 0x7D45) per project notes – flagging for the owner to re-measure with `?debug=1` at `?grid=320x120&lights=8` before this AC is taken as a hard pass/fail.** Not failing the story on this alone per test brief.
- [x] Regression: `check-deps.mjs` OK (108 files); `lighting.test.js` 115/115; `gpu/glsl.test.js` 33/33; `world/packed.test.js` 6/6; `gpu/WorldTextures.test.js` 16/16; `render/compositor.test.js` 10/10; `gpu/gpuCompare.test.js` 35/35; `physics.test.js`/`jump.test.js`/`eyeFeel.test.js`/`playerLook.test.js` all green; `check-deps.test.mjs` 8/8 (fixture test, its "FAILED" lines are the intentional bad-file fixtures it's built to catch).

Bugs: none found.

Performance: see perf AC above – GPU 5.40 ms at 320x120/8 lights/sun-on in this headless run (over the 4 ms budget); JS light pass 0.46 ms p50 (well under 2 ms). Needs an owner-hardware confirmation via `?debug=1&grid=320x120&lights=8` (F3 overlay, copy button) before treating the GPU number as final.

Console errors: none, across the whole session (sun on/off, F6/F7 spam, gpucompare ×2, lever pull + grate animation under live GPU rendering, CPU fallback).

Notes:
- Pointer lock never engages in this preview-pane environment (a documented, expected limitation noted in `engine/core/playerLook.js`'s own comments). Movement/look were driven via `window.__debug` (position/camera) and real `KeyboardEvent`s dispatched through `window` (the same `Input` listener target used in production) so F6/F7 and the lever interaction exercised the actual production code paths, not shortcuts around them.

Suggestions (non-blocking): none new; the architect's existing debt items (`?debug=light`/`?debug=sun`, LIGHT-buffer `?gpucompare=1` readback) stand as recorded in architecture.md 14.3 item 7.

**Owner real-GPU check (2026-09-24):** `?grid=320x120&lights=8&debug=1` -> gpu 1.46 ms, 60 fps, frame 0.85 ms. Budget (<= 4 ms) met; the headless 5.4 ms was software GL.
